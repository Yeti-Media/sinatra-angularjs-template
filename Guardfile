# A sample Guardfile
# More info at https://github.com/guard/guard#readme
#
#


guard :coffeescript, :output => 'public/app/js' do
  watch(%r{^client/js/.+\.coffee$})
end


guard 'sass', :input => 'client/sass', :output => 'client/css'
